import { PlanningBoard } from "@/components/planning/planning-board";

/** Planning — gestion de l'agenda + roster de l'équipe (docs/REFONTE-2.md §2.1). */
export default function PlanningPage() {
  return <PlanningBoard />;
}
